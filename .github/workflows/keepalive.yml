name: Keep Alive Render

on:
  schedule:
    - cron: "*/6 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping frontend
        run: curl -I --max-time 10 https://porfolio-lovato-matias.onrender.com

      - name: Ping backend
        run: curl -f https://portfolio-docker-stack-bakend.onrender.com/api/projects
